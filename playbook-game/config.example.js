// Optional: shared team leaderboard.
// Copy this file to config.js and set the URL of your deployed
// Cloudflare Worker (see README.md). Without config.js the game
// keeps a per-device leaderboard automatically.
window.BEES_API = "https://bees-board.YOUR-SUBDOMAIN.workers.dev";
